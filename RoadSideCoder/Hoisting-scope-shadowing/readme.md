## shadowing :

If we declare a variable in a global scope and then redeclare it in a local scope than the local scope declaration will be prioritize in the local scope. It is called the shadowing.

_Hoisting :_ We studied it in the Piyush's folder
